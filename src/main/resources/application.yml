spring:
  application.name: bookspot-crawler
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/BookSpot_sample
    hikari:
      username: root
      password: 1234
  jpa:
    #    show-sql: true  # 콘솔에 SQL 출력
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true